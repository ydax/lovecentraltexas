{
  "name": "quin",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "echo 'No build step required for CommonJS'",
    "deploy": "firebase deploy --only functions --project lovecentraltexas",
    "serve": "firebase emulators:start --only functions"
  },
  "dependencies": {
    "@genkit-ai/google-genai": "^1.22.0",
    "@modelcontextprotocol/sdk": "^0.5.0",
    "cheerio": "^1.0.0-rc.12",
    "cors": "^2.8.5",
    "express": "^4.18.0",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.4.0",
    "genkit": "^1.22.0",
    "genkitx-mcp": "^1.14.1",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "eslint": "^8.15.0",
    "eslint-config-google": "^0.14.0"
  }
}

